services:
  slim:
    image: ghcr.io/agntcy/slim:1.1.0
    container_name: slim-demo-slim
    ports:
      - "46357:46357"
    volumes:
      - ./slim-config.yaml:/config.yaml:ro
    command: ["/slim", "--config", "/config.yaml"]
    networks:
      - slim-demo

  collector:
    image: slim-otelcol:latest
    container_name: slim-demo-collector
    ports:
      - "8889:8889"
    volumes:
      - ./collector-config.yaml:/etc/otelcol/collector-config.yaml:ro
    environment:
      - SLIM_ADDRESS=http://slim:46357
    depends_on:
      - slim
    networks:
      - slim-demo

  prometheus:
    image: prom/prometheus:latest
    container_name: slim-demo-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yaml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
      - '--web.enable-lifecycle'
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - slim-demo

  grafana:
    image: grafana/grafana:latest
    container_name: slim-demo-grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_PATHS_CONFIG=/etc/grafana/grafana.ini
    volumes:
      - grafana-data:/var/lib/grafana
      - ./grafana.ini:/etc/grafana/grafana.ini:ro
      - ./grafana-datasources.yaml:/etc/grafana/provisioning/datasources/datasources.yaml:ro
    depends_on:
      - prometheus
    networks:
      - slim-demo

volumes:
  prometheus-data:
  grafana-data:

networks:
  slim-demo:
    driver: bridge
